card =  input(().sort())

for i in range(card):

    stack = [] #임시저장 장소
    count = 1  #횟수 

    if i[0] not i(card):
        stack.pop() #첫번째 조건 실행
        i[1] → i[n-1] #두번째 조건 실행 
        count += 1

    else i[0] = card :
        
        print(count)
